package com.inTrack.spring.repository;

import com.inTrack.spring.entity.PlaceOfAssemblyType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlaceOfAssemblyTypeRepository extends JpaRepository<PlaceOfAssemblyType, Long> {
}
